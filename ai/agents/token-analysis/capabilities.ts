export const TOKEN_ANALYSIS_AGENT_CAPABILITIES =
`The token analysis bot gets trading data for a given token.

It can get the top holders, the number of holders, the current price and volume, and the bubble map (which shows token distribution) of the token.`;
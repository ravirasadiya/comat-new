export const SOLANA_DEPLOY_COLLECTION_PROMPT = `Deploy a new NFT collection on Solana blockchain.

Required parameters:
- name: The name of the collection (e.g., "My Collection")
- uri: The URI for the collection's metadata (e.g., "https://example.com/collection.json")

Optional parameters:
- royaltyBasisPoints: The royalty basis points (e.g., 500 for 5%)`; 
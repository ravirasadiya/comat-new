export const SOLANA_REGISTER_DOMAIN_PROMPT = `Register a .sol domain name for your wallet.

Required parameters:
- name: The domain name to register (e.g., "mydomain" for mydomain.sol)

Optional parameters:
- spaceKB: The amount of space to allocate in KB (default: 1)`; 
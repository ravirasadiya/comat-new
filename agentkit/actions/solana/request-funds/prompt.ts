export const SOLANA_REQUEST_FUNDS_PROMPT = `Request SOL from Solana faucet (devnet/testnet only).

This action does not require any parameters. It will request SOL from the faucet for the agent's wallet address.
Note: This only works on devnet or testnet networks.`; 